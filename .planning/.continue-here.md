---
context: default
status: current
last_updated: 2026-07-21T18:25:00Z
---

# Continue Here

<current_state>
WaystoneOS is current after agent-observable manual preview findings.
The canonical project marker is `docs/development/CHECKPOINT.md`, and the
Phase 0/version 0.1 alignment marker is
`docs/development/PHASE-0-0.1-ALIGNMENT.md`.
The transfer-readiness marker is
`docs/development/PUBLISH-TRANSFER-READINESS-AUDIT.md`.
</current_state>

<completed_work>
Completed in recent implementation slices:

- Removable copy-time execution failures now produce structured failed or
  partial executor results instead of unrecorded command errors.
- `publish --execute-removable --json` includes optional per-file error text.
- The CLI exits nonzero when the transfer result is not `completed`, while
  still printing the structured executor result.
- Completed-history TOML is written from executor results for completed,
  failed, and partial copy-time outcomes.
- Manual completed-history helper inputs now accept `partial` as a transfer
  result value.
- Focused service and CLI tests cover deterministic partial copy failure.
- Added `publish --export-removable-state` to scan a selected removable target's
  configured destination root into the existing path-only local state manifest
  shape.
- Added planner and CLI tests showing the exported manifest can feed
  `--dry-run --remote-state` and classify destination files as skip/delete.
- Added CLI JSON contract smoke coverage for the new command.
- Added ADR-0014 for the `org.waystone.Publish1.ExecuteRemovable` D-Bus
  request/response shape.
- Cross-linked the shape from service contracts, publishing service, D-Bus
  adapter plan, transfer-readiness audit, checkpoint, and alignment docs.
- Implemented `org.waystone.Publish1.ExecuteRemovable` in
  `waystone-publishd` from the ADR-0014 shape.
- Added private-session-bus smoke coverage for rejected unsupported fields,
  missing confirmation, confirmed removable execution, copied files, and
  completed-history output.
- Added Qt Publish-pane read-only removable destination-state export through
  `publish --export-removable-state --json`.
- The Qt helper writes a timestamped project-local manifest under
  `history/previews/`, loads it into the existing remote-state comparison
  field, and refreshes preview/readiness/history comparison panes.
- Audited Phase 0/version 0.1 alignment after the publishing-heavy slices.
- Decided the next implementation lane should pivot from publishing/UI
  refinement to a repo-local Debian session/install-layout contract.
- Added ADR-0015 for the version 0.1 Debian session/install-layout boundary.
- Added repo-local session source artifacts under `session/`:
  `waystone.desktop`, `waystone-session`, and `install-layout.toml`.
- Added `scripts/session-layout-smoke.sh` to validate the session entry,
  future target paths, launcher argument forwarding, and missing-binary
  failure behavior without installing files outside the repository.
- Added `scripts/session-dev-smoke.sh` to build the Qt Workspace under `/tmp`,
  launch it through `session/waystone-session`, and verify default-root,
  explicit-config, and missing-root diagnostics.
- Added `scripts/install-layout-temp-root-smoke.sh` to stage the future
  session, service binary placeholder, D-Bus service, and systemd user-unit
  install tree under `/tmp` and verify path alignment against
  `session/install-layout.toml`.
- Added `scripts/run-dev-session.sh` to build the current Rust CLIs and Qt
  Workspace, then launch the preview through `session/waystone-session`
  without installing files.
- Added `docs/development/MANUAL-PREVIEW-CHECKLIST.md` with temporary-root
  manual preview setup, workflow checks, expected deferrals, and finding notes.
- Added `docs/development/MANUAL-PREVIEW-FINDINGS.md` recording default-root
  and explicit temporary-root dev-run diagnostics, offscreen startup, focused
  Qt smoke coverage, and the remaining need for project-owner visible
  inspection.
- Development, architecture, transfer-readiness, and Phase 0/0.1 alignment docs
  are current.
</completed_work>

<verification>
Checks passed in the current agent-observable manual preview findings slice:

- `git diff --check`
- documentation/status review
- `QT_QPA_PLATFORM=offscreen scripts/run-dev-session.sh --check-roots --no-user-config`
- `QT_QPA_PLATFORM=offscreen scripts/run-dev-session.sh --config "$preview_config" --check-roots`
- `QT_QPA_PLATFORM=offscreen timeout 5s scripts/run-dev-session.sh --config "$preview_config"` returned `124`, indicating the event loop remained active until timeout
- `scripts/workspace-qt-project-smoke.sh`
</verification>

<remaining_work>
Recommended next implementation step after resume:

1. Project owner runs the visible manual preview checklist and records
   findings.
2. After the visible pass, choose whether to fix preview issues, improve the
   dev-run/manual path, or plan the first explicitly approved installed-session
   slice.
3. Keep actual installation outside the repository, Qt mutating publish
   controls, real SSH transfer, remote deletion,
   credential unlock, remote verification, audio device enumeration, playback,
   and installed activation deferred.
</remaining_work>

<decisions_made>
- Keep feed generation local-only and Atom-only.
- Use prepared feed-entry sidecars as authoritative for matching Atom entry IDs.
- Preserve unrelated existing Atom entry XML without attempting a full XML
  editor or remote feed merge.
- Keep Qt Workspace on CLI adapters for now.
- Treat `record capture` as an explicit-input capture contract, not device
  discovery.
- Treat completed publication history as explicit local result recording, not
  proof that WaystoneOS performed a remote transfer.
- Treat `publish --validate` as local readiness validation, not remote probing
  or verification.
- Keep Qt Publish-pane validation display as a read-only CLI adapter surface.
- Treat `BuildCompletedHistory` as D-Bus result-record construction only.
- Treat `SaveCompletedHistory` as a local result-recording API, not proof that
  WaystoneOS executed remote publication.
- Treat Qt completed-history display as read-only list/detail, not a UI claim
  that WaystoneOS executed publication.
- Treat `--remote-state` as caller-supplied local comparison input, not a
  remote probe or proof of remote truth.
- Treat transfer readiness as an explicit gate before any remote mutation.
- Prefer a non-mutating transfer-intent contract before any executor.
- Treat `publish --transfer-intent` and `org.waystone.Publish1.TransferIntent`
  as execution-readiness reports only; they do not contact remotes, unlock
  credentials, transfer files, delete files, verify remotes, or write completed
  history.
- Treat `publish --prepare-removable-execution` as a non-mutating local
  executor preparation contract only; it does not copy files, create
  directories, delete files, write completed history, call D-Bus, or contact
  remotes.
- Treat `publish --execute-removable` and
  `org.waystone.Publish1.ExecuteRemovable` as local/removable file-copy
  execution only; they require explicit transfer confirmation, refuse upload
  overwrites, copy through destination-directory temporary files before
  renaming into place, write completed history from executor results, and do
  not execute deletes, contact remotes, unlock credentials, probe SSH host
  keys, or verify remote results.
- Prefer removable/local execution before SSH execution once executor work is
  approved.
- Treat Qt removable destination-state export as a local comparison helper: it
  writes only a project-local manifest under `history/previews/` and does not
  execute publish transfers or inspect remote hosts.
- Treat the next OS/session step as repo-local artifact definition and smoke
  verification only; do not install into system paths without explicit
  approval.
- Treat `session/waystone-session` as the stable future display-manager wrapper
  name for the Debian-hosted 0.1 preview.
- Treat `session/install-layout.toml` as a source contract, not an installer.
- Do not let ordinary `--yes` imply remote delete confirmation.
- Treat sibling Waystone applications as future add-ons only; do not edit
  outside this repository unless explicitly told.
</decisions_made>

<blockers>
- No blockers are known.
- No human actions are pending.
- No background servers, watchers, or async external jobs are running.
</blockers>

## Pause Marker

Current on 2026-07-21 after agent-observable manual preview findings. No OS
image, installer, installed service activation, display-manager registration,
or boot/loading path is ready for manual testing yet. The current Workspace can
be launched from the repo through `scripts/run-dev-session.sh`, and the manual
preview checklist/findings docs are ready for the project-owner visible pass.

## Required Reading

1. `ROLE.md` - project role and operating expectations.
2. `PLAN.md` - full project breadth and phase intent.
3. `docs/development/CHECKPOINT.md` - current implementation marker.
4. `docs/development/PHASE-0-0.1-ALIGNMENT.md` - current alignment audit.
5. `docs/development/IMPLEMENTATION-STATUS.md` - implemented behavior and
   verification marker.
6. `docs/development/PUBLISH-TRANSFER-READINESS-AUDIT.md` - transfer gates and
   next publishing slice.
7. `docs/decisions/0014-dbus-removable-executor-shape.md` - accepted mutating
   publish D-Bus request/response shape.
8. `docs/decisions/0015-debian-session-install-layout.md` - accepted Debian
   session/install-layout contract.
9. `docs/architecture/SESSION-INSTALL-LAYOUT.md` - current repo-local session
   and future install target paths.
10. `docs/architecture/AUDIO-METADATA.md` - current audio metadata/export/feed
   contract.

## Infrastructure State

- Repository root: `/media/nick/1TB_Storage1/projects/waystone-repos/waystoneOS`
- Branch: `main`
- Remote: `origin` on GitHub repo `njb1966/waystoneOS`
- No files outside this repository should be modified without explicit
  approval.

<next_action>
Project owner runs the visible manual preview checklist and records findings
before installed-session work.
</next_action>
