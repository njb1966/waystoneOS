---
context: default
status: current
last_updated: 2026-07-20T00:00:00Z
---

# Continue Here

<current_state>
WaystoneOS is current after the real Opus publication-copy export slice. The
canonical project marker is `docs/development/CHECKPOINT.md`, and the
Phase 0/version 0.1 alignment marker is
`docs/development/PHASE-0-0.1-ALIGNMENT.md`.
</current_state>

<completed_work>
Completed in the last implementation slice:

- Replaced the mock `record export-opus` implementation with `ffmpeg/libopus`
  encoding while preserving the existing CLI/service request and response
  shape.
- Added explicit encoder failure errors for missing/unusable `ffmpeg`.
- Updated Rust unit/integration tests to exercise encoded `.opus` output from a
  generated test WAV when `ffmpeg/libopus` is available.
- Updated the CLI JSON contract smoke and focused Qt recording smoke to verify
  `engine = "ffmpeg"`.
- Updated current architecture, service, Qt, checkpoint, implementation-status,
  and alignment documentation.
</completed_work>

<verification>
Checks passed in this slice:

- `cargo test`
- `cargo clippy --all-targets -- -D warnings`
- `scripts/cli-json-contract-smoke.sh`
- `cmake --build /tmp/waystone-workspace-qt-build`
- `scripts/workspace-qt-project-smoke.sh`
- `scripts/workspace-qt-smoke.sh`
</verification>

<remaining_work>
Recommended next implementation step:

1. Review the remaining 0.1 audio/feed gaps and choose the next
   CLI/service-first contract deliberately.
2. Strong candidate: add an explicit feed XML update/merge contract while
   keeping remote publishing deferred.
3. Alternative candidate: add a narrow local recording-capture contract if
   capture is now more important than feed polish.
</remaining_work>

<decisions_made>
- Use native Debian `ffmpeg` with `libopus` for publication-copy export instead
  of adding Rust codec dependencies.
- Keep Qt Workspace on CLI adapters for now.
- Treat sibling Waystone applications as future add-ons only; do not edit
  outside this repository unless explicitly told.
</decisions_made>

<blockers>
- No blockers are known.
- No human actions are pending.
- No background servers, watchers, or async external jobs are running.
</blockers>

## Required Reading

1. `ROLE.md` - project role and operating expectations.
2. `PLAN.md` - full project breadth and phase intent.
3. `docs/development/CHECKPOINT.md` - current implementation marker.
4. `docs/development/PHASE-0-0.1-ALIGNMENT.md` - current alignment audit.
5. `docs/development/IMPLEMENTATION-STATUS.md` - implemented behavior and
   verification marker.
6. `docs/architecture/AUDIO-METADATA.md` - current audio metadata/export
   contract.

## Infrastructure State

- Repository root: `/media/nick/1TB_Storage1/projects/waystone-repos/waystoneOS`
- Branch: `main`
- Remote: `origin` on GitHub repo `njb1966/waystoneOS`
- No files outside this repository should be modified without explicit
  approval.

<next_action>
After committing this slice, propose the next 0.1 implementation choice:
feed XML update/merge contract first, or narrow local recording capture first.
</next_action>
