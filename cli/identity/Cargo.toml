[package]
name = "waystone-identity-cli"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[[bin]]
name = "identity"
path = "src/main.rs"

[dependencies]
waystone-cli-output = { path = "../../crates/cli-output" }
waystone-host-identity = { path = "../../crates/host-identity" }
